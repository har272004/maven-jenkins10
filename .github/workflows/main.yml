name: My First CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Code checkout
        uses: actions/checkout@v3

      - name: Run echo
        run: echo "CI working!"
